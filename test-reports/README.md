# 转测报告归档

本目录用于按版本和算子归档转测报告。

目录结构：

```text
test-reports/
├── v26.1.0/
│   └── <operator>/
└── v26.6.0/
    └── <operator>/
```

归档约定：

- 第一层目录固定使用版本号。
- 第二层目录固定使用看板中的算子标准名称。
- 报告文件直接放入对应算子目录。
- 建议文件名使用 `YYYY-MM-DD_<operator>_<scene>.<ext>`。
- 报告中不得包含账号、Token、内部地址、绝对路径或其他敏感信息。
